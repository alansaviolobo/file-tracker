<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:bottomLeftRadius="70dp"
        android:bottomRightRadius="70dp"/> <!-- Adjust the radius as needed -->
    <solid android:color="@color/indigo_A200" /> <!-- Set your desired background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/black" />
            <corners android:radius="70dp" /> <!-- Button corner radius -->
        </shape>
    </item>
</shape>